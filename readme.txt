# Smart Energy System – Power Prediction

This repo contains the power prediction notebook and datasets.

## Structure
- `Data/` – raw input datasets (CSV)
- `notebook/powerprediction.ipynb` – main analysis & processing
- `processed/` – outputs written by the notebook (ignored in git)

## Run
```bash
pip install -r requirements.txt
jupyter notebook notebook/powerprediction.ipynb
